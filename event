{"kind":"report","text":"CHANGES-REQUESTED at exact clean pushed head da1d7ae1027aaee071cca2e0fe5460dbca15e8cd on request/item5-chess-ui, reviewed from a clean detached checkout; local and origin tips agree and the parent chain sits on chess main ebf683c. Most of this candidate is right, and two defects block it. WHAT PASSED. Item 5 is satisfied: the UI is embedded (go:embed, one binary), the board renders from the folded FEN, legal moves come only from the fold engine, motion hints are leased presence never durable, and the live room is bounded (host/live caps plus adapter caps, restart reset proved by test). SeatFor is the single non-mutating authority query: liveRole is gone, the hand-written turn map is gone, prepare/open/renew/motion all route through it, commit() stays only on the four fold paths after acceptance, and SeatFor operates on a game copy through a value receiver. The core pin is exact: go.mod pseudo-version resolves to identity merge 204b377c and LookupActorAt is genuinely called, not reimplemented. Gates all green from the detached checkout: go test -count=1, go test -race -count=1, go vet, gofmt -l, git diff --check. Mutation checks: exact-key precedence, sameIdentity scheme+subject, collision, frontier-instant choice, handler value-binding, and the side-to-move gate are each pinned — six mutations, each failing at least one test, restored clean after. DEFECT ONE, blocking: the mandated actor argument is unwitnessed and its violation is exploitable. Mutating chess.go:572 from LookupActorAt(fingerprint, lastRecordID) to LookupAt(lastRecordID) — resolving the frontier record own author instead of the queried fingerprint — leaves the ENTIRE suite green, and a probe shows a never-endorsed stranger key then inherits the frontier actor seat: SeatFor returns white,true, runtime.role hands the browser the seat, and validateMotion lets a watcher publish motion as a player. The cause is structural: the foldThrough test helper truncates the log so the last record IS the queried act, making the two calls identical by construction. Required: a regression where the queried fingerprint is deliberately not the frontier record author, failing under the LookupAt substitution. DEFECT TWO, blocking: README. Line 8-9 still claims the module imports only host and host/identity, but this head adds host/live — the module-boundary paragraph is now false. The preview-versus-judgment caveat exists only in the SeatFor doc comment and is absent from the README, which condition 5 requires; and the new loopback-only --listen refusal is undocumented. SECURITY, recorded non-blocking: unauthenticated observe with wait_ms drives roughly 120 whole-log folds per request times 32 waiters (loopback-only but unbounded in log length); runtime.chat has no cap on game keys; live.ActorFingerprint errors are discarded and fail closed only by accident; GET routes sit outside trustedLiveHost (fold data only, mirrors the core resident pattern). SIMPLIFICATION, recorded: foldMove reimplements SideToMove byte-for-byte and the substitution passes the suite — collapse it so the fold gate and live gate cannot drift; runtime.seatFor is a mutable function-pointer indirection in production existing for one test that already value-binds; the prepare response computes and returns a role that app.js never reads, an unauthenticated authority answer worth deleting. ARCHITECTURE: affected layers are the chess fold/projection (new exported SeatFor and SideToMove, internal re-signing of seatSide/matchSeat), the cmd/chess adapter (new web layer), and the core dependency now including host/live; no gitseq core contract changes; the chess governing document is README.md, which is exactly where defect two bites. Fix the two defects — the regression test and the README corrections — and the rest stands; the simplification items are recommended while the head is open, the SideToMove collapse verified free.","body":{"artifact":"git:sha1:1b96687964cbba5a6089c526d7136b32ba9488a5#git:sha1:d5aebce9df64ff958cfb5551ffe573903c5cf1a5","head":"da1d7ae1027aaee071cca2e0fe5460dbca15e8cd","verdict":"changes-requested"}}