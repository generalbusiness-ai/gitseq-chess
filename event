{"kind":"assert","text":"The separately authorized local CLI/MCP experiment proves a supported transport gap at delivered e7a65612. With a disposable two-player game, serve owns the repository writer: CLI board and MCP show_board work, but both agent e7e5 write attempts refuse another Chess writer owns this repository, leaving the board unchanged. Stopping serve permits the same MCP move/idempotency key to succeed; that writing MCP process then owns the writer and serve cannot restart until MCP exits. The restarted HTTP endpoint sees the accepted reply. Thus the current documented clients require stopping the human web service for an agent turn, and stopping the writing agent process to resume the service. The OS lock correctly enforces single ownership and must not be bypassed. Nineteen diagnostic steps and actual RPC/HTTP results are attached; all owned processes were reaped, keys remain private in the disposable directory, and no source or public service was changed. This is a material playability finding, not browser acceptance; the actual browser review remains blocked by the separately recorded plugin failure. A focused follow-up request will commission planning for an agent transport through the owning service."}