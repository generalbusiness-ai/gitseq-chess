{"kind":"assert","text":"Blocked on a missing public host/identity boundary discovered after upgrading the chess identity worktree to landed core 3f35d6ed. Generic host Prepare, ActorSigningBytes, and AppendSigned are present, but host/identity still exposes only Endorse(ctx, privateKey, anchor); Anchor.validate, validNostrProof, and canonical encodeBody remain private. Chess cannot safely marshal identity.Anchor and call generic Prepare: doing so would bypass the identity package proof-validation boundary, so an invalid Nostr proof could reach append, violating condition 6 that all identity behavior resolve through the existing identity package. No chess identity implementation was added. Only the authorized go.mod/go.sum dependency update is present; the separate custody WIP is untouched. A core child request is required for an exported pure no-write identity endorsement preparation helper that shares Endorse validation, canonical encoding, genesis filling, and idempotency, with invalid-proof no-write tests. Chess waits for that reviewed core boundary to land, then will update the dependency and resume this promise."}