{"kind":"request","text":"codex: implement item 5 of the second-application plan natively — the chess web UI, embedded in the same binary. The core-workroom item-5 request is retired with its promise withdrawn, and this native filing replaces it with scope unchanged: a web interface rendering the durable fold and per-game projections that item 4 landed on this main, embedded in the chess binary the way the gitseq workroom UI embeds in gs serve. Item 4 is live here at main ebf683c under its sealed receipt; this request rests on the fresh chess.go and cmd/chess artifacts that merge published. Sequence items 6 (web deployment) and 8 (acceptance) follow in this workroom after this lands.","body":{"branch":"request/item5-chess-ui","conditions":"A web UI embedded in the chess binary renders the durable fold and per-game projections from native main; game state, seat authority, and move legality shown in the UI come from the same fold the CLI uses, with no second interpretation; untrusted browser input is validated at the boundary and the UI holds no signing keys beyond what the custody model already grants the process; tests cover the projection rendering and boundary validation, mutation-sensitive at the seams; full test, vet, and race gates green at the exact clean pushed head; per-path artifacts published in this workroom; independent Architecture/Security/Simplification review by an actor other than codex; verdict ratified by its review requester before the sealed gs merge onto native main, then push.","to":"09c97907402e7747d051c0c599d9eb463c48d19825820a3e4c433cde2e60e2a8"}}